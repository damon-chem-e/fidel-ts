# Tuna mirror: pip install -i https://pypi.tuna.tsinghua.edu.cn/simple <xxx包>

# ============================================================================
# INSTALLATION OPTIONS:
# ============================================================================
# 
# This file contains ALL dependencies for a complete installation.
# For modular installation, see the requirements/ directory:
#
# Minimal install (CPU, no LLM, no GPU specific dependencies):
#   pip install -r requirements/base.txt
#
# With LLM support:
#   pip install -r requirements/base.txt -r requirements/llm.txt
#
# With GPU specific dependencies:
#   pip install -r requirements/base.txt -r requirements/gpu.txt
#
# Full install (everything - same as this file):
#   pip install -r requirements.txt
#
# ============================================================================

# Core dependencies - install these first
numpy==1.26.4
torch>=1.13.0
packaging
ninja

# Standard dependencies
einops>=0.8.0
matplotlib>=3.9.0
pytorch_lightning>=2.0.0
pmdarima>=2.0.0
ptflops>=0.7.0
# ray==2.37.0  # Not used in codebase - remove if not needed
scikit-learn>=1.5.0  # Only uses StandardScaler/MinMaxScaler (stable APIs)
sentence_transformers==3.2.0  # Requires specific transformers version
thop==0.1.1.post2209072238  # Specific post-release version likely needed
tqdm>=4.66.0
fastparquet
pandas
transformers>=4.41.0,<5.0.0
pyyaml>=6.0
tensorboard>=2.8.0 
typer>=0.9.0
rich>=13.0.0
pydantic>=2.0.0
wandb>=0.16.0  # Experiment tracking and logging (optional but recommended)

# LLM-related dependencies (optional - see requirements/llm.txt)
# Install with: pip install -r requirements/base.txt -r requirements/llm.txt
vllm==0.7.2  # 0.8.3 provides reasoning compatability but consumes more memory
fschat[model_worker,webui]
flashinfer-python
openai

# Build-heavy dependencies (optional - see requirements/gpu.txt)
# Install with: pip install -r requirements/base.txt -r requirements/gpu.txt
nvitop
chronos-forecasting
