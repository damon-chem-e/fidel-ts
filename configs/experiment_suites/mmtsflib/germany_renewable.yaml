# Experiment Suite: MMTSFlib on Germany_renewable
# Description: Train MMTSFlib (prediction-level late fusion) with FEDformer on Germany_renewable dataset
# Usage: python -m cli.suite run configs/experiment_suites/mmtsflib/germany_renewable.yaml
# Note: Germany_renewable uses hourly sampling (downsampled to -h)
#       Input: 360 samples = 15 days (half month), Output: 168 samples = 1 week (second smallest)
#       Split points: {Jan. 1, 2021, Jan. 1, 2022}
#
# IMPORTANT: This suite requires pre-computed LLM embeddings!
# Generate embeddings first:
#   python -m cli.inference generate-suite configs/experiment_suites/mmtsflib/germany_renewable.yaml

suite:
  name: "mmtsflib_germany_renewable"
  description: "Train MMTSFlib with FEDformer on Germany_renewable dataset with 15-day context, 1-week prediction"
  tags: ["mmtsflib", "multimodal", "late-fusion", "fedformer", "pytorch", "fidel-ts", "germany_renewable"]

  # Execution settings
  execution:
    parallel: false
    continue_on_error: true
    log_dir: "./logs/suites/mmtsflib/germany_renewable"

  # Experiments in this suite
  experiments:
    - name: "mmtsflib_fedformer_germany_renewable"
      description: "MMTSFlib with FEDformer on Germany_renewable with 15-day context, 1-week prediction"
      enabled: true
      template: "configs/templates/pytorch_training.yaml"
      overrides:
        model:
          name: "MMTSFlib"
          config_path: "model_configs/general/MMTSFlib.yaml"
        data:
          name: "fidel_Germany_Renewable_Power_Grid:fullGRPG_hetero_TGTSF_H"
          config_path: "data_configs/Germany_Renewable_Power_Grid/fullGRPG_hetero_TGTSF_H.yaml"
        training:
          input_len: 360      # 15 days at hourly intervals (half month)
          output_len: 168     # 1 week at hourly intervals
          batch_size: 256
          learning_rate: 1e-3
          patience: 10
          epochs: 50
          track_per_sample: false
          evaluate_test_during_training: false
          truncate_train_for_purge: true
          torch_compile: true
          compile_mode: "reduce-overhead"
          use_tensor_cache: false  # Disable tensor cache
        device:
          use_gpu: true
          gpu: 0
          use_multi_gpu: false
          devices: "0"
        wandb:
          enabled: true
          tags: ["mmtsflib", "germany_renewable"]
        llm_embedding:
          model_name: "gpt2"
          cache_dir: "./LLM_cache/"
          extraction_mode: "last_token"
          max_length: 512
          prompt_template: "mmtsflib_v1"
          # Memory-efficient mode for large Fidel-TS dataset
          memory_efficient: true
          batch_size: 64
          chunk_size: 1000
        model_config_overrides:
          unimodal_model_type: "FEDformer"
          seq_len: 360
          pred_len: 168
          enc_in: 1           # Germany renewable has 1 target
          d_model: 512
          n_heads: 8
          e_layers: 2
          d_layers: 1
          d_ff: 2048
          modes: 32
          mode_select: "random"
          moving_avg: 25
          embed: "timeF"
          freq: "h"
          input_text_dim: 768
          prompt_weight_mode: "fixed"
          prompt_weight: 0.01
          pool_type: "avg"
        experiment:
          type: "pytorch"
          output_dir: "outputs/suites/mmtsflib/germany_renewable"
