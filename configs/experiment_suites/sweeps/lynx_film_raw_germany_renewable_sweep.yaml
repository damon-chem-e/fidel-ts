# Experiment Suite: lynx_film_raw Germany Renewable Power Grid Sweep
# Description: Hyperparameter sweep for lynx_film_raw on Germany Renewable Power Grid dataset
# Usage: Used with wandb sweep for architecture (d_model) and training (LR, batch_size) parameters
# NOTE: Sweeps around parameter values: LR=0.00025, batch_size=8, d_model=512, e_layers=2, n_heads=8

suite:
  name: "lynx_film_raw_germany_renewable_sweep"
  description: "Hyperparameter sweep: lynx_film_raw on Germany Renewable Power Grid (architecture + training params)"
  tags: ["lynx_film_raw", "multimodal", "pytorch", "fidel_ts_sweep", "film", "germany_renewable"]

  # Execution settings
  execution:
    parallel: false
    continue_on_error: true
    log_dir: "./logs/suites/lynx_film_raw_germany_renewable_sweep"

  # Experiments in this suite
  experiments:
    # Germany Renewable Power Grid - hyperparameter sweep with architecture parameters
    - name: "lynx_film_raw_germany_renewable_sweep"
      description: "lynx_film_raw on Germany Renewable Power Grid (HYPERPARAMETER SWEEP - architecture + training params)"
      enabled: true
      template: "configs/templates/pytorch_training.yaml"
      overrides:
        model:
          name: "lynx_film_raw"
          config_path: "model_configs/general/lynx_film_raw.yaml"
        model_config_overrides:
          input_text_dim: 768  # BERT embedding dimension (projects 768 -> text_dim=256)
          # d_model, n_heads will be overridden by sweep
          n_heads: 8
          e_layers: 2
          text_film_last_n: 1
        data:
          name: "Germany_Renewable_Power_Grid"
          config_path: "data_configs/Germany_Renewable_Power_Grid/fullGRPG_hetero_TGTSF_H.yaml"
        training:
          use_tensor_cache: true  # Enable tensor cache for fidel-ts datasets
          text_embedding_stride: "full"  # Full resolution text embeddings (recommended)
          truncate_train_for_purge: true  # Remove lookahead bias, making val_loss reliable
          input_len: 360        # Context length
          output_len: 168       # Prediction length
          batch_size: 8       # Will be overridden by sweep (default in middle of range)
          learning_rate: 0.00025  # Will be overridden by sweep (default in middle of good range)
          lradj: constant      # Keep learning rate constant (no exponential decay)
          patience: 5
          epochs: 30
          track_per_sample: false
          evaluate_test_during_training: true
        wandb:
          tags: ["lynx_film_raw", "germany_renewable", "sweep"]
          system_monitoring: true
          system_log_interval_s: 30.0
          system_sample_interval_s: 0.5
          batch_log_interval: 10
          validate_every_n_batches: 10
          validate_num_val_batches: 1
        experiment:
          type: "pytorch"
          output_dir: "outputs/suites/lynx_film_raw_germany_renewable_sweep"
