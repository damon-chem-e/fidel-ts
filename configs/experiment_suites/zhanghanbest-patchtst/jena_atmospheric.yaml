# Experiment Suite: ZhangHanBest (PatchTST) on Jena_Atmospheric_Physics
# Description: Train ZhangHanBest with PatchTST encoder on Jena_Atmospheric_Physics dataset

suite:
  name: "zhanghanbest_patchtst_jena_atmospheric"
  description: "ZhangHanBest (PatchTST encoder) on Jena_Atmospheric_Physics with 15-day context, 1-week prediction"
  tags: ["zhanghanbest", "patchtst", "multimodal", "pytorch", "fidel-ts", "jena_atmospheric"]
  
  execution:
    parallel: false
    continue_on_error: true
    log_dir: "./logs/suites/zhanghanbest_patchtst/jena_atmospheric"
    
  experiments:
    - name: "zhanghanbest_patchtst_jena_atmospheric"
      description: "ZhangHanBest with PatchTST encoder on Jena_Atmospheric_Physics (15-day context, 1-week prediction)"
      enabled: true
      template: "configs/templates/pytorch_training.yaml"
      overrides:
        model:
          name: "ZhangHanBest"
          config_path: "model_configs/general/ZhangHanBest.yaml"
        model_config_overrides:
          unimodal_model_type: "PatchTST"
          seq_len: 360
          pred_len: 168
          d_model: 512
          revin: true
          input_text_dim: 768
        data:
          name: "Jena_Atmospheric_Physics"
          config_path: "data_configs/Jena_Atmospheric_Physics/fullJAP_hetero_TGTSF_H.yaml"
        training:
          text_embedding_stride: "full"
          input_len: 360
          output_len: 168
          batch_size: 256
          learning_rate: 0.0001           # Time series model LR
          projector_learning_rate: 0.01   # Residual projection LR (100x higher per Zhang et al.)
          lradj: constant
          patience: 5
          epochs: 50
          track_per_sample: false
          evaluate_test_during_training: false
          truncate_train_for_purge: true
          torch_compile: false
          compile_mode: "default"
          use_tensor_cache: true
        wandb:
          tags: ["zhanghanbest", "patchtst", "jena_atmospheric"]
        experiment:
          type: "pytorch"
          output_dir: "outputs/suites/zhanghanbest_patchtst/jena_atmospheric"

