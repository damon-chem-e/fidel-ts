# Experiment Suite: lynx_film on Jena_Atmospheric_Physics
# Description: Train lynx_film model on Jena_Atmospheric_Physics dataset using pretrained iTransformer checkpoint
# Usage: python -m cli.suite run configs/experiment_suites/lynx_film/jena_atmospheric.yaml
# Note: Jena_Atmospheric_Physics uses hourly sampling (downsampled to -h)
#       Input: 360 samples = 15 days (half month), Output: 168 samples = 1 week (second smallest)
#       Split points: {Jan. 1, 2021, Jan. 1, 2022}
# NOTE: pretrained_model_path contains a placeholder that must be filled in before running

suite:
  name: "lynx_film_jena_atmospheric"
  description: "Train lynx_film model on Jena_Atmospheric_Physics dataset with 15-day context, 1-week prediction using pretrained iTransformer"
  tags: ["lynx_film", "multimodal", "pytorch", "fidel-ts", "jena_atmospheric", "film"]
  
  # Execution settings
  execution:
    parallel: false
    continue_on_error: true
    log_dir: "./logs/suites/lynx_film/jena_atmospheric"
    
  # Experiments in this suite
  experiments:
    - name: "lynx_film_jena_atmospheric"
      description: "lynx_film on Jena_Atmospheric_Physics with 15-day context, 1-week prediction using pretrained iTransformer"
      enabled: true
      template: "configs/templates/pytorch_training.yaml"
      overrides:
        model:
          name: "lynx_film"
          config_path: "model_configs/general/lynx_film.yaml"
        model_config_overrides:
          # Model shape and hparams must match pretrained iTransformer from experiment_suites/itransformer
          d_model: 128      # Match iTransformer d_model
          d_ff: 128         # Match iTransformer d_ff
          e_layers: 2       # Match iTransformer e_layers
          n_heads: 8        # Match iTransformer n_heads
          # TODO: Fill in pretrained_model_path with actual checkpoint path from itransformer pretraining
          # Expected path format: outputs/suites/itransformer/jena_atmospheric/{experiment_id}/checkpoints/best_checkpoint.pth
          pretrained_model_path: "PLACEHOLDER_FILL_IN_LATER_outputs/suites/itransformer/jena_atmospheric/PLACEHOLDER_EXPERIMENT_ID/checkpoints/best_checkpoint.pth"
        data:
          name: "Jena_Atmospheric_Physics"
          config_path: "data_configs/Jena_Atmospheric_Physics/fullJAP_hetero_TGTSF_H.yaml"
        training:
          input_len: 360      # 15 days at hourly intervals (half month)
          output_len: 168     # 1 week at hourly intervals (second smallest: {24, 168, 336, 720})
          batch_size: 768
          learning_rate: 8.66e-4
          patience: 5
          epochs: 50
          track_per_sample: false
          evaluate_test_during_training: false
          truncate_train_for_purge: true  # Remove lookahead bias, making val_loss reliable
          torch_compile: true  # Enable torch.compile for faster training
          compile_mode: "reduce-overhead"  # Recommended mode
        wandb:
          tags: ["lynx_film", "jena_atmospheric"]
        experiment:
          type: "pytorch"
          output_dir: "outputs/suites/lynx_film/jena_atmospheric"
