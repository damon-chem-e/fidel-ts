# Experiment Suite: LeRet on Germany_Renewable_Power_Grid
# Description: Train LeRet model on Germany_Renewable_Power_Grid dataset with text embeddings
# Usage: python -m cli.suite run configs/experiment_suites/leret/germany_renewable.yaml
# Note: Germany_Renewable_Power_Grid uses hourly sampling (downsampled to -h)
#       Input: 360 samples = 15 days (half month), Output: 168 samples = 1 week (second smallest)
#       Split points: {Jan. 1, 2021, Jan. 1, 2022}
#       Uses y_hetero (text embeddings) with timestamp_semantics: t_about

suite:
  name: "leret_germany_renewable"
  description: "Train LeRet model on Germany_Renewable_Power_Grid dataset with text embeddings, 15-day context, 1-week prediction"
  tags: ["leret", "retention", "pytorch", "fidel-ts", "germany_renewable", "text-embeddings"]
  
  # Execution settings
  execution:
    parallel: false
    continue_on_error: true
    log_dir: "./logs/suites/leret/germany_renewable"
    
  # Experiments in this suite
  experiments:
    - name: "leret_germany_renewable"
      description: "LeRet with text embeddings on Germany_Renewable_Power_Grid with 15-day context, 1-week prediction"
      enabled: true
      template: "configs/templates/pytorch_training.yaml"
      overrides:
        model:
          name: "LeRet"
          config_path: "model_configs/general/LeRet.yaml"
        data:
          name: "Germany_Renewable_Power_Grid"
          config_path: "data_configs/Germany_Renewable_Power_Grid/fullGRPG_hetero_TGTSF_H.yaml"
        training:
          text_embedding_stride: "full"  # Full resolution (LeRet may not use hetero data, but safe to include)
          input_len: 360      # 15 days at hourly intervals (half month)
          output_len: 168     # 1 week at hourly intervals (second smallest: {24, 168, 336, 720})
          batch_size: 128
          learning_rate: 8.66e-4
          patience: 5
          epochs: 20  # Total epochs (pretrain + finetune)
          track_per_sample: false
          evaluate_test_during_training: false
          truncate_train_for_purge: true  # Remove lookahead bias, making val_loss reliable
          torch_compile: true  # Enable torch.compile for faster training
          compile_mode: "reduce-overhead"  # Recommended mode
          # LeRet-specific two-stage training
          leret:
            training_stage: "both"
            pretrain_epochs: 10  # 50% of total epochs (10/10 split)
            pretrain_loss: "mse"
            finetune_loss: "mse"
            pretrain_learning_rate: 1e-3
        wandb:
          tags: ["leret", "germany_renewable"]
        experiment:
          type: "pytorch"
          output_dir: "outputs/suites/leret/germany_renewable"
        model_config_overrides:
          # Original LeRet hyperparameters
          d_model: 128
          n_heads: 8
          e_layers: 3
          d_ff: 256
          patch_len: 16
          stride: 8
          padding_patch: end
          revin: True
          affine: False
          subtract_last: False
          decomposition: False
          kernel_size: 25
          individual: False
          dropout: 0.05
          fc_dropout: 0.05
          head_dropout: 0.0
          # Text embedding configuration
          use_language: True
          language_dim: 768
          # Task type for text-guided forecasting
          task: TGTSF
          # Dataset-specific (enc_in will be inferred automatically)
          seq_len: 360
          pred_len: 168
