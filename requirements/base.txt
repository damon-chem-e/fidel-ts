# ============================================================================
# BASE REQUIREMENTS - Core dependencies for CPU/local development
# ============================================================================
# Install with: pip install -r requirements/base.txt
#
# This includes all core functionality without LLM or GPU-heavy dependencies.
# Perfect for local CPU development and testing.
# ============================================================================

# Core dependencies - install these first
numpy==1.26.4
torch>=1.13.0
packaging
ninja

# Standard dependencies
einops>=0.8.0
matplotlib>=3.9.0
pytorch_lightning>=2.0.0
pmdarima>=2.0.0
ptflops>=0.7.0
scikit-learn>=1.5.0  # Only uses StandardScaler/MinMaxScaler (stable APIs)
sentence_transformers==3.2.0  # Requires specific transformers version
thop==0.1.1.post2209072238  # Specific post-release version likely needed
tqdm>=4.66.0
fastparquet
pandas
transformers>=4.41.0,<5.0.0
pyyaml>=6.0
tensorboard>=2.8.0 
typer>=0.9.0
rich>=13.0.0
pydantic>=2.0.0
wandb>=0.16.0

# Testing (optional but recommended)
pytest>=7.0.0

