tgtsf test is a bit slow. lynx will also be slow. fixed the pretrained model path for lynx test. 

a few speedups are in the works: (1) don't evaluate on test every epoch, (2) don't do per sample metrics tracking every epoch, we'll do it in a separate evaluation script after the model is fully trained, (3) we tried to speed up the patch reconstruction look in tgtsf (didn't seem to help much).

>>>>>>>start training : 20251206-163409_c6abf40d0870>>>>>>>>>>>>>>>>>>>>>>>>>>
Loading train datasets ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% • 87/87 0:01:01
Loading val datasets ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% • 87/87 0:00:22
Loading test datasets ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% • 87/87 0:00:59
[ info ] Buffer cleared
Epoch 1/50 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% • loss: 0.7103125 • speed: 0.7805s/iter • ETA: 1858.7m 0:38:13
[12/06/25 17:14:52] INFO     Epoch: 1 cost time: 2293.69s
Validating... ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% 0:09:55
[GPU Monitor (last 30s)] Avg Util: 9.6%    Avg Mem: 10.17 GB   Max Mem: 10.17 GB   Mem Total: 44.99 GB
                         Avg Power: 106W   Max Temp: 39°C      Time: 17:37:47
Testing entities ━━━━━━━━━━━━━━━━━╺━━━━━━━━━━━━━━━━━━━━━━  44% 0:13:26
  └─ 387         ━━━━━━━━━━━━━━━╸━━━━━━━━━━━━━━━━━━━━━━━━  39% 0:00:09salloc: Job 6981423 has exceeded its time limit and its allocation has been revoked.
[GPU Monitor (last 30s)] Avg Util: 8.1%    Avg Mem: 10.17 GB   Max Mem: 10.17 GB   Mem Total: 44.99 GB
                         Avg Power: 102W   Max Temp: 39°C      Time: 17:38:18
Testing entities ━━━━━━━━━━━━━━━━━╸━━━━━━━━━━━━━━━━━━━━━━  45% 0:13:56
  └─ 385         ━━━━━━━━━━━━━━━━━━━━━━━━━━━╸━━━━━━━━━━━━  69% 0:00:16srun: error: node3302: task 0: Killed

looks like before (1-2) implemented a poch takes about 40 min to train and 10 minutes to validate (we aren't going to eval on test so we ignore that.) 6 hours on mit_normal_gpu gives maybe 6 epochs for a 6 hour run, then we just continue from there. Need to look at wandb to see how to pick up from where we left off for a given run and implement that (also in experiment manager, how to resume a given experiment and put the pause and resumption points in the metadata and keep everything in the same experiment folder)


FiLM is sketched, but not tested. Could probably start testing it on a L40S and debugging it after I implement the above and test the above then get tgtsf and lynx running on long pause-resume runs on the test (single prediction window for NYC_traffic dataset). After FiLM and residual learning, we work on gating.

---

speedups implemented.

Job resumption is pushed (not tested), and the first 6 hours is running for both lynx and tgtsf tests. While those are training (on lynx branch) I'll debug FiLM implementation (on a film branch for worktrees in damon-workspace). 

Should clean up wandb runs... 

FiLM ran into some issues with relative paths since it's the first I'm running in a worktree. That was debugged for a bit and pushed (and pulled into the worktree) but we need to test the latest push that added the base_data_path config option and set it for lynx_film_test.yaml. Hopefully that works. test on salloc interactive, and if it works, then keep testing film interactively to see what comes up and fix it up to the point it can get a 6 hour run going.

there was another issue with the relative paths for experiment management that made lynx test not create the right experiment directory structure, but I believe we've fixed that on the lynx branch. Seems to have worked fine interactively. Fixed, then started a 6 hour run on sbatch.

---

testing lynx_film_test and testing tgtsf_test resumption.
forgot to put the resumption information in lynx test so it's starting again from scratch, which is fine. we'll resume from this one (the previous one only did 3 epochs anyways)

pushed (and pulled into fidel-ts main worktree on engaging) some changes to fix up suite resumption. untested, but will test on the next stage of training lynx and tgtsf.

lynx_film had an error with tensors not on device. fixed and pushed locally, need to pull on engaging worktree and test again.

---

set up lynx_test to resume and started it again. tgtsf_test is also running again and doing quite well on the test dataset. 

seems like lynx (obviously) started faster, but it might end not as good as tgtsf. curious why. it might need a more aggressive learning rate? Letting it run and seeing if it does end lower.

could be that we need to add gating. but before adding gating, perhaps look at the distributions of unimodal vs multimodal after those runs complete. Also see how film does.

now try looking at per sample metrics (get them first) then work on analysis to see if gating makes sense before implementing gating.
also try doing raw lynx film. might want to spin up gpus elsewhere to keep all balls rolling (prototype and test elsewhere, and keep long runs on engaging, maybe eventually spin a lot of jobs up also on runpod when doing sweeps and all of the datasets).

once I have per sample metrics working and raw film testing, then we start doing irrelevant text injection. make the text look similar to the text in the dataset (basically just provide it to an LLM and ask it to generate something plausibly similar but not including the same location perhaps), then inject at random times (poisson arrival) from a dataset of irrelevant text generated via LLM api. Can't let it be so simple that it can just look for a location token or weather. Just make it want to look similar but different location, or not about the weather (or even about the weather, no restrictions to be different with some probability). Can label these irrelevant but looks relevant text samples. This is interesting because we are controlling what is and is not real / relevant. Gives a controlled environment to test gating before going to real noisy environments where we don't know ex ante what is signal and what is noise. I really really like this.

I think right now my priority is to get preemptable jobs up, merge lynx and film to get the experiment manager improvements everywhere, put the merged in a new worktree (so I can keep the current experiments running without interruption with disruptive changes), get film raw running, then start irrelevant text injection. Then work on getting good performance on that with possibly gating, possibly residual learning.

---

tgtsf_test ran, and it probably could still have a little juice but it's mostly done. lynx_test finished (early stopping). lynx_film_test is running, as is lynx_film_raw_test, neither on preemptable, but they will be moved to preemptable next. init on login node then run on another partition in resume mode is working.

still debugging per sample metrics after training (pretty close) -- the job was killed. This actually takes a while. Schedule more than an hour (probably two) for an interactive job to run this. It's not the per sample metrics that is the issue, it's really the forward pass. It's probably best to let per sample metrics run while training to avoid forward pass overhead (it isn't really a drag on training). But certainly do this still (interactive) on the jobs that we have already run (and are running).

lynx and film are merged into lynx.

Now while we wait on film jobs, I build the irrelevant text injection, after which we test that on the single test dataset I've been using, then after playing with that I decide what experiments I want to send to all fidel-ts datasets and do a big run. Before doing the big run, probably set up a sweep on the dataset I'm playing with and maybe one more. Those sweeps will inform the hparams for the big run of experiments (for which I might recruit runpod jobs as well).

noiseinjection has an initial commit and a new worktree locally (laptop). That worktree is on the new branch with the new plan and chat. Can work from there.

---

mmitransformer branch has sequence length and no sequence length text encoders, and my own version of mmitransformer with channel description prepending and K global text variate support. It also has the original mmitransformer. we will wait on that until we have a full run of film going and after noiseinjection is done (priorities).

---

lynx film  and lynx are not going to be worth it to run for film paper. we just use lynx film raw and tgtsf and the other benchmarks. we would have to train a unimodal model on the time mmd stuff and its just not worth it. ignore those for now. We learned our lesson and can put that in a blog post somewhere sometime down the road linking to rohan and lucas beyer's work.

time mmd seems functional. on to ttc and mtbench (goal: friday). then on to getting the benchmarks down (goal: weekend)

---

friday: ttc basically functional, just need to finish out filtering ttc medical for the ones that don't have enough given input len output len and split. plan made, just need to implement and test with ttc_test.yaml suite. Then we bring in the benchmarks (weekend).

---

saturday: ttc finished and functional. plan made for ansari model(s) and embeddings improvements. those should be done ideally sunday, and by 23d we ideally have informer, fedformer, time-lllm, timecma, leret, timesfm-2.5, chronos, tirex, tempo, tiny-tsm, pfn-ts (should be straightforward to get the pretrained from HF, and the others should be straightforward ports given source code.). Goal is to have that by 22d (monday) and 23d (tuesday) set up configs and initialize runs then set going on preemptable.

---

sunday: embeddings improvements worked on and trying to more deeply understand the data provision. need to test and ensure it works properly, then implement ansari (should be pretty easy the model, the embedding updates were needed to be able to get average aggregated embeddings for ansari). Ideally that's done monday, and by tuesday we have the benchmarks, and set up some runs.

---

monday: embeddings test appears to be working on fidel ts. haven't re-tested ttc or time-mmd. still need to do that. Also now that average embeddings are working we can try the zhanghanbest test, and also start bringing in the rest of the benchmarks. then we can test them, then set up runs. 

---

12/28: ttc and time-mmd are running great. those were not broken. now testing zhanghanbest (ansari) and bringing in the rest of the benchmarks. 
- zhanghanbest works on patchtst and dlinear, but does not work on sundial or time-moe due to cached code incompatibilities (you really have to make a repository and dependencies compatible with a pretrained, and that's time consuming.). so we will ignore those for now when implementing benchmarks including zhanghanbest. I have a plan in a chat to possibly create a separate set of requirements for time-moe, and a new option in dataloader for sundial to make these compatible, but that's lower priority than the non-foundation model benchmarks.
- informer and fedformer are in the works. required adding some time features. testing fedformer, haven't tested informer yet. fedformer is pretty close to working, but should keep fixing (maybe a couple more bugs). after those are worked out, I think informer will be pretty straightforward since it uses the same temporal marks / time features as fedformer that are new to the repo.

---

01/02: fedformer and informer functional. leret functional on the benchmarks_leret branch. benchmarks branch has llm embeddings and is working on TimeCMA and Time-LLM and has general llm embedding support. Currently working on debugging `python -m cli.inference generate time_mmd_traffic model_configs/llm_embedding/gpt2.yaml` to get it working, then need to try other datasets to generate, then test timecma experiment suite, then test time-llm. Then we get jobs churning.

for 01/03, should really get some jobs going for models that do run fine now on a worktree on engaging and get them going. that can be done in parallel to time-llm and timecma, and should get started now.

---

01/03: timecma working on time mmd datasets. llm embedding working, but takes a long time ane memory breaks on fidel-ts. need to fix that. time llm working on it, then going to set up and do the runs.

having an issue where we ran out of disk space on chimera_proj. I need to remove a bunch of stuff in MM-TSFlib and mts (checkpoints) which means I need permissions all transferred to me from people who were previosuly in the project, and remove those people from the project. also increase quota. talked to helpdesk and I have to wait for STS on monday AM.  for now going to try running from orcd/scratch (copying entire fidel-ts to there then working there til monday morning)

I currently have something with a venv and data on ~/orcd/scratch/fidel-ts. Can work there. Still need to test the `python -m cli.inference generate-suite configs/experiment_suites/timecma_test.yaml` to make sure it works there (everything works except the fidel-ts datasets, which are now configured to use chunking which is new and untested). `python -m cli.suite run configs/experiment_suites/timecma_test.yaml` also works, except the fidel-ts dataset. Test that, and also simultaneously test time-llm.

timecma_test llm embeddings done on orcd/scratch/fidel-ts for everything but the fidel-ts dataset (nyc traffic speed), and that has a job queued for 6 hours on a h200 via sbatch.

time llm also was taking forever on the forward llm pass (smallest gpt2 variant 124M params) so I gave it 6 hours on an l40s to try to see how far it gets, also via sbatch.

mmtsflib is implemented but not tested. need to run the llm embeddings with the new prompt (on time mmd only for now), then run the test (on time mmd only for now). 

I think by sunday we should have time-llm functional. timecma is functional except some stuff with memory on fidel-ts. trying to fix that tonight (saturday). THen starting the week we actually run everything. Also on Monday we put film in MMVision, test small, and run it in that repository.

I think sunday first priority is to set up and start the runs for film raw on everything. one job per fidel-ts dataset, and one job for all of time mmd and ttc, all of those jobs initialized then put on preemptable. then back to getting time-llm and timecma up as well as mmtsflib.

---

01/04: set up runs for film raw on all datasets in configs/experiment_suites/lynx_film_raw/ and need to initialize then run those, but I want to do that *off scratch* so I wait til monday when engaging engineers can transfer permissions and expand quota to actually run these jobs (and destroy the old checkpoints from mmtsflib and mts that are hogging disk space)

progress tracking and resumption for chunked llm embeddings untested and currently running 6 hour job on sbatch.

mmtsflib appears to be working with llm embeddings. time llm appears to be working.

tomorrow is fixing up chimera_proj (permissions and quota), running the fidel-ts jobs, then getting the other runs up and going (including on runpod where I have credits -- get through it all fast). need to verify that my implementations somewhat match original papers where tested on the same data, then run on fidel-ts datasets. As part of that, check whether my completed tgtsf run matches the original fidel ts paper. also monday is implementing and testing film on mmvision.

the plan for film implementation is ready in MMVision. just get opus to implement, then test on h200. for vision we won't show the other methods, just ours to illustrate that it works.

---

01/05: timecma_llm_embedder put the llm_embeddings under the time series directory (but that's where it looks for them) so it's all good. I thought it wasn't working but it was, just I was looking in the wrong place. Verification of the progress works as well. Running the completion (train done, now working on val then test, hopefully done after the next 6 hour run. resumption looks good from the log.). I need to copy that llm embeddings directory back to chimera_proj after it completes in orcd/scratch.

I got permissions fixed and 10 TB quota on chimera_proj, so moving back there. Can start jobs again.

initializing fidel-ts lynx film raw runs on chimera_proj, but before I run those I want to hparam opt film on nyctraffic since the fidel-ts reported fiats is 0.407 and I'm not getting that, maybe some hparam opt can help us, particularly since the first epoch does all the work. working on getting hparam opt up since I need to be beating the reported in fiats on fidel ts. some hparam opt on nyctraffic, then apply those changes to the other fidel-ts, get it better than reported in fidel-ts, then we are in business.

mmvision film is under implementation, then will test on engaging.

---

01/06: mostly working on sweep management particularly with job resumption due to slurm cluster. going to test those tomorrow on test sweeps (see test resumption sweep plan in context), then get actual sweeps on a couple time mmd and ttc. Then we will sweep nyc traffic on fidel ts. once we are happy with performance, we do the full runs with good hparams. then we get runs going for all the benchmarks on fidel-ts that we don't have from other papers. We also run fidel-ts on the time-mmd and ttc. 

mmvision film also needs to be tested, and that can be done in parallel.


---

for time mmd traffic, looks like 0.0002-0.0003 learning rate, 8 batch size, d_model 512, e_layers 2, and n_heads doesn't really matter. more for low batch size, less for high batch size.

trying ttc medical now. 

working on simultaneous runs in a single location (engaging on my user, brianna's user, and phoebe's user) so we can run several jobs simultaneously for fidel-ts sweeps. in sweeps-lock branch.

time mmd looks way way too good. looks like there was some lookahead bias. fidel-ts has a way to have known future events (like scheduled downtime) and that is usable in training to predict. I think we put the historical stuff (not known in advance) into that category (assumed known in advance) when doing time mmd. perhaps fidel-ts is fine and time-mmd is not fine. if that is the case, then clearly we need to fix time mmd and make the documentation much, much, much, much, much more precise and clear on these points and change the nomenclature of these inputs and make clear documentation about them.
 
---

01/10: fixing the timemmd and ttc lookahead bias issues and giving very clear documentation on timestamp semantics.

once that is fixed, will merge in sweeps_lock [done], re-run time-mmd and ttc sweeps (testing multi-with-lock [done]), then ensure we are doing decent compared to benchmarks from original papers (also run the unimodal from original mmtsflib / time-mmd and ttc papers to see if those are also doing oddly well inside my repo, or if it's just lynx film raw). we know we are outperforming fiats on fidel-ts already so we are good there.

then we spin up hparam sweeps on fidel-ts and run those. in parallel we keep tuning on timemmd and ttc until we are beating the benchmarks from original papers. we also run the benchmarks we want on on fidel-ts that are not run in the fidel-ts paper. 

deadline to have all that spun up is monday 01/12. and have it spinning on runpod and with brianna and phoebe's users on mit as well as my users on preemptable.

things I can do while waiting for implementation: make a list (and configs and sbatch scripts) for the benchmarks to run on fidel-ts.

working on test set evaluation from experiment configs and suite configs at the moment.

for 01/11: I think the main thing is to just take the good looking config from the sweeps on ttc and time mmd with our fixes, and run it across all time mmd and ttc. Should be quick. Compare to the original papers and ansari. Also run raw iTransformer and compare to the original papers and ansari. then set up the runs for lynx-film-raw and benchmarks on fidel-ts and get those going and ask phoebe and brianna to spin up some runs on their users. Basically now it's spinning up and monitoring jobs. Fixes are basically complete, except the cli fixes for evaluation on test, which I will work on while runs are going (spinning up runs is higher priority.)

---

01/11: test set evaluation is working now. I found the lookahead bias from the lack of a purge period between train and val, making val look too good, but test looks like the original papers (phew). we also yesterday fixed the y_hetero vs x_hetero and timestamp_semantics issues. We have added thorough documentation of all of these issues. 

I am currently looking into using the same train/val/test splits as original papers (for consistency), but only training on [:-pred_len] of the train split to avoid the lookahead into the val set. I need to run the benchmarks on fidel-ts, and I need to run lynx-film-raw on all of time mmd and ttc. 

I am concerned that lynx-film-raw is performant on large training data, but not on small training data. Perhaps with small training data (as I had originally imagined), the llms win out (closer to zero shot) like time-llm and informer and others. First things first, get everything done then I have some results. Once I have initial results, then we ensure we hparam tune benchmarks on fidel-ts to ensure we are playing apples-to-apples, and then we try tweaking lynx-film-raw on smaller data sets perhaps shoving in the text generated by timecma, informer, etc. into the text input to see if that is helpful. I think also a mental intuitive model of all the benchmarks will be useful to compile to understand the differences once we have the results.

---

01/12: see running_experiments.md for progress.

I think a couple plots that would be nice would be average text sequence length vs performance, showing that I do well when there is a lot of text. Somewhere I am not doing well is not outperforming unimodal always on time mmd and ttc. Looking into options for that. I could show lynx_film (not lynx_film_raw), which is a nice result in itself (and ablation) showing that learning the residual is helpful, but you get better performance when text is useful by learning the entire signal. I would need to train lynx_film and lynx_film_raw on everything. Also looking into a hybrid with opus that could learn the entire signal *and* not underperform unimodal. 

Something else possibly worth looking into is applying to patchtst.

I think for now priority before running lynx_film_raw on fidel-ts is to get a few more sweeps on nyc traffic. I need a trained itransformer unimodal baseline on each fidel-ts dataset, so I should start those for sure and keep those checkpoints saved. Then I can run lynx_film on everything to compare. Note that this is far more important on time mmd and ttc than on fidel-ts, so actually let's get those itransformer results on time mmd and ttc first. Maybe a quick sweep for itransformer parameters. 

planned results:
- [point A] We know already that FIATS outperforms unimodal on fidel-ts test sets (except CAISO, where it underperforms). So we show that we do more efficient and more performant on these big datasets where we know multimodal is helpful, and compare to the other multimodal methods and show the loss curves and show we are better than other multimodal and multimodal outperforms unimodal.
- [point B] Then for time mmd and ttc we show that we can beat unimodal every time, which multimodal consistently can't do as shown in ansari (zhanghan). However, time mmd method (prediction level ensemble) consistently does better multimodal than unimodal (naturally). But, we show that we do better than time mmd method on the big datasets (point A).
- [point C] we show that learning a residual can be damaging to performance, and learning the full signal is better. 
- [point D] we show that prediction level ensembles with unimodal baseline are useful (just like time mmd method), and present other methods for ensuring that text can be used, but does not pollute time series signal when alternative modality isn't useful. Hypothesis is that prediction level ensembles are most useful with low data, and the other methods can outperform with high data. We show ablations on various ways to mix with gating.

so in summary we do better than alternatives when data is available (point A), and we consistently beat unimodal (point B). We show that learning a residual loses information (point C) and it's better to learn the full signal. We also show that prediction level ensembles are useful and present other methods for ensuring that the text signal is useful but not hampering the time series signal (point D).

I think the priorites for high compute usage are the following:
- (2) tgtsf loss curves on fidel-ts
- (1) itransformer pretraining on fidel-ts
- (3) other benchmarks on fidel-ts (leret, timecma, time-llm, zhanghanbest-patchtst, zhanghanbest-dlinear, mmtsflib-fedformer)
- (4) lynx_film_raw initial on fidel-ts
- (5) lynx_film on fidel-ts (after pretraining complete)
- (6) lynx_film_enhanced select configurations on fidel-ts (after testing, low compute ablations, and itransformer pretraining complete)
- (7) mmvision best configuration of lynx film enhanced or lynx film raw

low compute quick runs:
- (1) itransformer pretraining on time mmd and ttc [training complete, evaluation pending]
- (2) lynx_film on time mmd and ttc [running]
- (3) lynx_film_enhanced ablations on time mmd and ttc

experimentation:
- (-1) feed the gpu [in progress]
- (0) lynx_film_enhanced working with all configurations
- (1) mmtsflib working with fedformer (performant in original paper) -> able to run on fidel-ts
- (2) zhanghanbest working with patchtst and dlinear (performant in original paper) -> able to run on fidel-ts
- (3) mmvision best configuration of lynx film enhanced or lynx film raw


high compute mapping:
- [mit_preemptable, l40s x 4] [running] sweep lynx_film_raw nyc traffic (limit until wednesday)
- [sched_mit_sloan_gpu_r8, a100] [running] timellm on fidel-ts
- [sched_mit_sloan_gpu_r8, a100] [running canada, OOM caiso] timecma on fidel-ts (embeddings first)
- [runpod, l4 x 6] [need to spin more up] itransformer pretraining on fidel-ts, using the bash configs (from original branch in workspace)
- [runpod, a40 x 6] tgtsf (fiats) on fidel-ts, using the bash configs (from original branch in workspace)
- [runpod, rtx 4000 ada x 5] leret on fidel-ts
- [runpod, l40s x 5] lynx_film_raw initial (nyc_traffic already complete)
... (completion)
- [tbd, x5] lynx_film (after pretraining complete)
- [tbd, x6 x nconfigurations] lynx_film_enhanced configurations (after testing and itransformer pretraining complete)
- [tbd, x6] zhanghanbest-patchtst (after experimentation) 
- [tbd, x6] zhanghanbest-dlinear (after experimentation)
- [tbd, x6] mmtsflib-fedformer (after experimentation)
- [tbd, x1] mmvision best configuration of lynx film enhanced or lynx film raw

low compute mapping:
- [mit_normal_gpu, l40s] low compute quick runs
- [mit_normal_gpu, l40s] experimentation
- [runpod, 1x] bumping gpu utilization up (testing, don't scp over results and don't log to wandb, use a config set to disable wnadb)

rest of today:
- mapping of compute to jobs for high compute usage
- start of high compute jobs
- test and verify lynx_film_enhanced functions

tomorrow: 
- low compute quick runs on one mit_normal_gpu 
- experimentation on the other mit_normal_gpu (lynx film enhanced -> mmtsflib + zhanghanbest)
- get that bumping gpu utilization up working

runpod -> engaging:
- runpod jobs will complete on their pod, then the results must be scp-ed to engaging (output subfolder recursive) so that other jobs can use them. 
- a storage volume will contain the persistent storage of data and output for all runpod jobs, and will be attached to all pods (very cheap per month). this allows pods to be spun up and deleted at will without having loads of scp.
- spin pods on community cloud for pricing and availability
- one pod per fidel ts dataset, and run everything on that dataset on that pod (for storage reasons)
- use <command> | tee /workspace/command.log to log output 
- also use `capture-pane -S - -E -; save-buffer /workspace/session.log` after cntl-b to save entire tmux session to a log file
- to scp, use `scp -P <port> -i ~/.ssh/id_ed25519 root@<IP>:/workspace/session.log ~/Downloads/` if the exposed TCP ssh command given in runpod is `ssh root@<IP> -p <port> -i ~/.ssh/id_ed25519`

compute efficiency:
- runpod shows that I'm using 20% cpu utilization and 0% gpu utilization. nvidia-smi shows 3-4% gpu utilization on average. this is for lynx_film on canada. I need to figure out how to bump that utilization waaaayyy up. that will make jobs go significantly faster and I can iterate faster. I'm using a lot of VRAM, but something is cpu bound, not gpu bound (bad)

---

01/15: tensor cache debugging. optimizing disk space and memory usage during cache generation. fixed issues with number of news items and shapes into models. fixed some issues with torch compile and noncontiguous arrays in models (all of lynx film raw time mmd ttc seems to run now). found and fixed an issue with hetero striding, and am ensuring everything has full text resolution.

an issue: running each time mmd and ttc individually -> no contiguous errors. running them sequentially in suite -> contiguous errors.
an issue: jena atmospheric -> cuda errors (related to many channels) -- solved. 

a note: different batch sizes can use the same tensor cache.
a note: the contiguous error has a chat open with auto and we can implement and test the time mmd ttc again to ensure it runs.
a note: we can validate that tensor cache exists compatible with a job before submitting the job.
a note: the contiguous progress should make it possible to use the pytorch compile on the high compute jobs.

goals this afternoon:
- tensor cache generated and trained from cache with high gpu utilization and good speed. (done)

goals later today: 
- have a runpod instance per fidel-ts dataset up, each running their respective tensor caching. 
- have timecma llm embedding completed on the A100s (done) and start the MMTSFlib llm embeddings on the A100s. 
- remove torch compile from ttc and time mmd and run those with full resolution on lynx_film_raw and lynx_film (itransformer doesn't need to be rerun). 

state 1 am 01/16: 
- tensor cache being generated on runpod-nyc-traffic (estimate: 2 days) and on runpod-germany (estimate: overnight) going to make sure it works on germany then spin up the rest of the pods.
-- creating a plan to have tests to ensure a new tensor cache implementation matches the current one, and to be able to use that to write one in polars that would be 100x more efficient. if we can get that working tomorrow, we can iterate on tensor cache much faster (an hour maybe rather than 50 hours for nyc traffic) which would help us ensure we can get the deadline in the case we need to regenerate tensor cache
- lynx_film_raw time mmd and ttc worked great on orcd/scratch and the resumption IDs are in the config, but cli.test suite (eval) still having issues -- working through that.
- mmtsflib works great with itransformer as unimodal, but fedformer as unimodal is still having some issues.
- still need to get to testing lynx film enhanced, mmvision, and zhanghanbest.

---

state 01/16 11 am:
- tensor cache polars and direct data access optimizations lead to massive speed ups (20x) in tensor cache generation. -- nyc traffic speed on track to finish cache generation in like 10 minutes
- using tensor cache in training on canada photovoltaics appears to be working great (but needed to reduce batch size since many channels on canada photovoltaics) -- canada photovoltaics doing about one epoch in 12 minutes and consistently at 50% gpu utilization on an ada 5000 (14% VRAM) so this could fit on a smaller GPU (half of ada 5000)
 
state 01/16 11 pm:

worktree monitor:
- pytorch compile spinner
- cli live monitors for step and batch level loss and gpu utilization, vram, cpu utilization, ram (these should all be time on x-axis and dynamic -- ideally a package exists -- wandb LEET)

worktree tensor_cache_polars (in lynx home locally):
- work on the experiment tracking and planning runs, and getting things that are broken to run properly.
- create an orchestrator that does the following: runs training on a suite. then pulls up the saved metadata and experiments in that suite, runs evaluation on that suite and tees the results to the experiment directory. right now cli.test suite requires resumption ids. I think this makes sense as a flag in cli.suite run like --include-evals. evaluation is also not loading the right configs (model shapes) as training, making cli.test not work. this should address that and fix it. 

principles:
- get all initial runs done first before hparam optimization. hparam optimization feels feasible with speedups.
- ideally we have all initial runs done with wandb offline and monitorable with wandb leet, then wandb sync.
- once we have initial runs and some hparam optimization, we can also try the lower prediction horizon (lower priority)

some planning:
- before FIATS, lynx_film_raw, lynx_film runs and itransformer runs, we want the tensor caches generated. then we go sequential itransformer -> lynx_film, and lynx_film_raw can be done immediately. for lynx_film_raw keep early stopping really low like 2-3 since we know it should early stop fast. save compute.

- time llm and timecma we don't really care. they don't use it. just do those on the Al00s. keep them going (only on fidel-ts).

- we need to get mmtsflib-fedformer, zhanghanbest-patchtst, and zhanghanbest-dlinear working fine on interactive -> can set up the runs. those can be sequentially after itransformer, lynx_film, lynx_film_raw.

- then we get lynx_film_enhanced working -> get those runs (possibly on new pods to ensure we get them done)

- for mmtsflib, we can run llm embedding before we actually have it running with fedformer.

- lynx_film_raw with the tensor cache on nyc traffic speed looked like the val loss was ridiculously low (like zero). not sure why. having opus look into that. hopefully not a big but with tensor cache.

- for comparison with fidel-ts, we will need to keep the hetero stride in a version, and also have a version with full resolution to show the gains. the ones we will really care about are the ones with the full resolution. For the hetero stride, just compare FIATS and my models of choice. for the other benchmarks we use full resolution.

- we are going to need to go through experiments saved to output and see whether they were generated with full resolution or not (for example, early timellm runs may not have had full resolution, though later might have had. just an example, I don't really remember if timellm uses hetero stride). Part of evaluating results.

- test monitor branch on ttc -> merge into tensor_cache_polars

so right now running:
- timellm, mmtsflib llm embeddings (A100s)
- [before I sleep] tensor cache generation for each fidel-ts dataset with the bug fix (on engaging)

tomorrow running:
- on mit_normal_gpu: make sure the test after train works seamlessly (important for all subsequent runs)
- on mit_normal_gpu: test monitor -> use it on runpods (important for all subsequent runs, and for paper graphics and statistics)
- ensure that we can use tensor cache for hetero stride
- on mit_preemptable: FIATS (full resolution); FIATS (original resolution)
- on mit_preemptable: leret (full resolution)
- port tensor caches to runpods -> itransformer -> (check against fidel-ts paper) -> lynx_film_raw -> lynx_film (on runpods, all full resolution) -> lynx_film_raw (original resolution)
- on mit_normal_gpu: look at whether lynx_film_raw and lynx_film stack up on time mmd and ttc against papers -> if not test lynx_film_enhanced -> lynx film enhanced ablations on time mmd ttc (with results all stored and tabulated) -> best on runpods
- on mit_normal_gpu: make sure eval works on the completed timellm runs -> get the evals -> ensure reasonable 
- on mit_normal_gpu: mmtsflib-fedformer, zhanghanbest-patchtst, zhanghanbest-dlinear working -> submit jobs with tensor cache on engaging or runpods, whichever free
- on mit_normal_gpu: fix the resume error for nyc traffic for timellm and get that on A100 -> fix the timecma issues and get timecma running on A100s
- on mit_normal_gpu test mmvision (H200) -> run on mit_normal_gpu (H200)

---

01/17 noon:
- leret configs up, looking at running. tensor caches all generated. need to sleep before doing the monitor and test after train testing -> fiats, lynx_* runs. 
- mmtsflib, timecma, zhanghanbest all require some sleep before experimentation then running. those we do the select on big datasets, and we don't need to run on small (because have from other papers)
- lynx_film_enhanced + looking at full resolution lynx_film_raw and lynx_film on time mmd ttc will be nice after tensor cacheing time mmd and ttc (that functionality is in alwaystest) and having test after train on. easy to see what's going on. add some functionality to always test to print a nice results table (train/val/test normalized mse on all suites) after suite completion (suite manager level). will be nice for iteration through lynx_* to get good performance on ttc and time mmd.
- the lynx_film and lynx_film_raw (after itransformer) on the runpods on fidel-ts are the initial runs. hopefully it's pretty quick to run, and we can hparam tune and run again after doing the iterations on previous bullet on ttc and time mmd. quick compute allows this. iterate fast.
- for the benchmarks run before tensor test after train (leret, timellm), need to make sure that evals work and get those evals and make sure they look reasonable, but we don't care too much honestly.
- mmvision is a little lower on priority list, don't worry about it til these other essential things are resolved.

---

01/17 11:30 pm: 
- timellm has nan loss for all but caiso. probably something in how the prompt output is translated or something. look into it and rerun. 
- no nan loss for leret -- appears to run great.
- tonight want to test the alwaystest branch and monitor branch, merge, push (test in orcd/scratch). but before that we get timellm, timecma working so those can churn tonight (tensor_cache_polars branch -- run in chimera_proj). if mmtsflib and zhanghanbest can also churn tonight on large, that would be incredible, then all the benchmarks are complete on large data. then I have my 'competition' on all datasets and can focus on lynx_* iteration [working on timellm [current] -> timecma -> mmtsflib -> zhanghanbest]
  - timellm appears to work fine with some improvements to param initialization, learning rate, etc. going to run again, but need to first ensure that training stops on nan. resubmitted.
  - timecma: looks like it works without torch compile. tested on time mmd. *however*, llm embeddings are required here, and llm embeddings are not supported in tensor cache yet. I need to make a new branch tensor_cache_llm (to avoid breaking stuff) where I support llm embeddings vs other embeddings in the hash (also the llm embedding prompt in the hash), then generate tensor caches for large datasets with the llm embeddings, then run timecma with the tensor caches to keep it from clogging up compute and taking forever (and to make it re-runnable)
  - mmtsflib: running interactive with tee on configs/experiment_suites/mmtsflib_test.yaml --filter "fedformer" to produce an error and bring local -> it's in fidel-ts/context/logs/mmtsflib_fedformer_0118.log -> claude working on a fix [looks like it might just be config overrides for fedformer knowing if data is monthly or hourly, test on an hourly fidel-ts]... note that mmtsflib will need to be in the tensor_cache_llm branch, and will require a new tensor cache (distinct from the cache for timecma since the prompt is different).
  - zhanghanbest: ...

note on itransformer: might need some significant improvements on hparams to really get performance (which is needed for a solid unimodal baseline for lynx_film and lynx_film_enhanced). 
- probably learning rate and learning rate schedule are quite important here. width/depth/etc also might be important. also batch size.

---

01/18 4 am:

- (2) check up on timellm, leret, itransformer (pretrain)
- (3) mmtsflib and timecma require the tensor_cache_llm branch -> test (mmtsflib problems see above) -> tensor cache generations (for both prompts) on preemptable -> trains on preemptable
- (4) test zhanghanbest -> run on preemptable
- (1) take a look at itransformer outputs and check against original fidel-ts paper -> hparam tune if needed
- (5) test monitor + alwaystest -> FIATS (full resolution) -> FIATS (original resolution) -> lynx_film_raw -> lynx_film_raw (original resolution) [runpods] 
- if itransformer good -> lynx_film [runpods]


- (6) on mit_normal_gpu: look at whether lynx_film_raw and lynx_film stack up on time mmd and ttc against papers -> if not test lynx_film_enhanced -> lynx film enhanced ablations on time mmd ttc (with results all stored and tabulated) -> best on runpods
- (7) [unnecessary now, I can see the test results in the sbatch_logs] on mit_normal_gpu: make sure eval works on the completed timellm,leret runs [some config overrides issues perhaps, was having trouble loading the checkpoint] -> get the evals -> [still need to compile the evals from sbatch_logs and ensure reasonable] ensure reasonable 
- (8) on mit_normal_gpu test mmvision (H200) -> run on mit_normal_gpu (H200)

---

01/18 3 pm:
- leret ran mostly fine a couple need to keep going
- timellm also ran great except nyc traffic ran into nans again at epoch ... and it didn't stop on nan. looking into how to prevent the nan for this one (config) and make sure it actually stops on nan (claude tensor cache polars), made changes, pushing, and trying again.

[timellm] right now I'm working on getting that timellm working (fixed and resubmitted), then checking on monitor and alwaystest on orcd/scratch (monitor is checked, just adding a couple more features to alwaystest). then I can merge and test, then open the pods and run itransformer -> etc etc while leret, timellm run on cluster, then mmtsflib and zhanghanbest run on cluster.

[monitor] monitor with wandb leet appears to work great with some odd problems at the end, but I'm working on fixing that. also want to learn wandb leet better. looks like wandb leet doesn't really need customization, just hotkeys to filter panes if I want. fixed I think, testing now. works great.

[alwaystest] looks like alwaystest seems to do fine with the summary things at the end. the test jsons are saving good. looks like everything is working as intended. also itransformer is wicked fast with torch compile. found that test is showing in the summary tables as N/A for normalized MSE, but it is findable in the metrics/test_results.json... so I'm not sure what's going on. It would also be nice to have that printed after the experiment in the CLI (currently no sign of testing on test in the CLI)

[best epoch new issue - alwaystest] something that's a bit off is saving the best epoch. I need to fix that, probably in the alwaystest branch. when earlystopping saves a checkpoint, it can update the best epoch. right now it just always seems to be the last epoch (false)


01/19 1:20 am
[agent hooks] this is not in a branch, but is going to be a new repo. will add to a new moot public github that I will have (and rigorously save the auth info) for open source stuff. working on this in browser with opus, and it's coming along nicely. when the hook wasn't set up correctly it caused claude code to hang. removed the global hooks. will try on a single project (maybe on the agent-monitor project iteself) with a project specific .claude/settings.json then see if it hangs and if it works. if it does, then we move it to global.

[always test] merged with monitor, tested and it works nicely. need to merge into tensor cache polars (careful with configs) then port to the pods, then we run loads of things on the pods.

- (1) check up on timellm, leret
- (2) merge tensor_cache_polars and alwaystest -> port tensor caches to pods (spin up) -> itransformer runs -> lynx_film_raw
- (1) take a look at itransformer outputs and check against original fidel-ts paper -> hparam tune if needed -> lynx_film
- (3) tensor_cache_llm branch development -> test -> tensor cache generations (for both prompts) on preemptable -> trains on preemptable
- (4) test zhanghanbest -> run on preemptable
...
- (5) on mit_normal_gpu: look at whether lynx_film_raw and lynx_film stack up on time mmd and ttc against papers -> if not test lynx_film_enhanced -> lynx film enhanced ablations on time mmd ttc (with results all stored and tabulated) -> best on runpods
- (6) on mit_normal_gpu test mmvision (H200) -> run on mit_normal_gpu (H200)

---

01/19 1 pm

merged tensor_cache_polars into alwaystest. pushed the new alwaystest into chimera_proj and orcd/scratch. timellm is completed. leret has three still running.

[tensor-cache-llm] claude is churning on this in the new branch and worktree. I think that it's not worth it to generate the llm compatibility for tensor cache. going to just run mmtsflib and timecma without tensor cache (changed configs on alwaystest). if that takes too long we come back to it. actually... if we can keep it fully contained and separate in the tensor_cache_llm branch and never merge that back (only use it for timecma and mmtsflib), then the risk of breaking other things is negligible and we can just try it. it's too slow without tensor cacheing. maybe we let claude churn on it a bit after all...claude is now churning on tests, after which he'll churn on working on test driven development. TDD (test driven development) complete. Need to test on the cluster.

[alwaystest] looks like the merge with tensor_cache_polars rolled back some test after train progress, so trying to fix that in the alwaystest claude, then will test again on interactive gpu, then roll out to the pods. the fix works. rolled out to nyc traffic pod and running itransformer there.

[agent-monitor] claude is messing about in a convo with hooks enabled on itself, reading its debug log, and trying to get the hooks to work. looks like the hooks aren't hanging anymore which is good, but it's still not writing to status. got it working, just working out some kinks (currently doesn't update after receiving approval/disapproval for permissions).

[pod-monitor] gemini is churning on getting some runpod api stuff that will help me monitor my pods (spot vs on demand, if spot is it interrupted or not, if stopped is it available to spin back up) 

[itransformer] my itransformer nyc traffic run got precisely (like exactly) the same test loss as the original fidel ts paper! This is great. We are in business.

[pod:nyc-traffic:stopped] itransformer run, lynx_film run, lynx_film_raw is run. (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). ported results to chimera_proj.

[pod:canada:terminated] itransformer run, lynx_film and lynx_film_raw run. (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). ported results to chimera_proj

[pod:germany:stopped] setting up (uv done, git cloned, porting over the germany data in progress, tmux complete). itransformer run. lynx_film and lynx_film_raw run (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). ported results to chimera_proj.

[pod:caiso:stopped] setting up (uv done, git cloned, porting over the caiso data done, tmux complete). itransformer run. lynx_film and lynx_film_raw run. (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). ported results to chimera_proj.

[pod:jena:stopped] setting up (uv done, git cloned, porting over jena data complete, tmux complete). itransformer run. lynx_film and lynx_film_raw run. (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). ported results to chimera_proj.

[pod:bear_room:stopped] setting up (uv done, git cloned, tmux complete, porting over bear room data complete). itransformer run. lynx_film run. lynx_film_raw run (had to reduce to 256 batch size, using the itransformer just run, using tensor cache). results ported to chimera_proj. 

[tgtsf (static_embedder_fix)] looks like this is quite memory hungry. probably going to run on preemptable rather than on pods and leave pods for the lynx film work. submitted jobs on preemptable for full resolution. once those are done submit for original resolution. nyc traffic, germany, and canada all have one variable... or maybe not. churning with claude to ensure we have the correct fix on this, then need to document which runs need to be re-done, and roll out the origres jobs again that failed.

[timecma] waiting on tensor_cache_llm testing

[mmtsflib] waiting on tensor_cache_llm testing

[leret fix] cursor churning on a fix

[zhanghanbest] added a hidden layer in residual projection, which matches paper better. testing on jena on orcd/scratch. generated configs for zhanghanbest-patchtst and zhanghanbest-dlinear. testing on jena on orcd/scratch (with tensor cache): zhanghanbest-patchtst and zhanghanbest-dlinear run great. 

[static embeddings fix] I will need to rerun jena, caiso, bear_room on lynx_* models with the fix. nyc_traffic, germany, canada are unaffected and don't need to be rerun. The reruns will tell us how important channel descriptions are. models that don't use variable descriptions (everything except tgtsf and lynx_*) don't need to be rerun.

[enhancements] caiso is horrid for lynx_film and lynx_film_raw. worse on val than test by a mile, and worse on test than val by 10 miles. I think perhaps we need to to multiple different CV splits over the train data (not respecting temporal ordering -- helpful if the distribution shifts) and early stop when any of those or several have val loss going up. I think it massively overtrained (10 epochs). Or, simpler solution: just train one epoch always. another possibility is the gating (lynx film enhanced). if val loss is worse than unimodal, perhaps we could in lynx_film_raw use the val set to inform the gate? like some held out portion and if it's bad or worse than unimodal that signal goes somehow to the gate. I also think that looking at the text data is useful. It could be that when the text data is highly structured film does quite well, but when it is not then film is too direct and fails (overfits). oddly, lynx_film_raw did amazing with nyc traffic in initial runs, but in full resolution it did terribly (way overfit). Curious whether this has to do with original resolution vs full resolution. need to dig into the results more on the runs that worked vs the run that drastically overfit to see what went wrong.

[enhancements] lynx_film does horrid on full resolution usually (at least on nyc traffic and bear room). original resolution seemed to be better for nyc traffic, but lynx film raw I think will do better. probably lynx film overfits (which fits what we are seeing) the residual.

[mmvision] ...

---

for 10/20 morning:
- test zhanghanbest -> get running on pods when available
- get claude cooking on tensor cache llm (better to at least try it, it won't finish without tensor cache probably) + figure out the sbatch_logs/timecma_* errors -> run timecma + mmtsflib
- while claude is cooking: analyze results so far and work on getting high performance on my own models everywhere -- iterate fast and ensure we beat benchmarks
- mmvision 

get the agent monitor working is a side quest (currently uv run stuff)

---

01/20 10 am:

- tgtsf_fullres_* is queued on mit preemptable
- leret is still running on preemptable
- tgtsf_origres_* is queued on mit sloan
- lynx_film_raw finishing up on pods

01/20 2 pm:

- we might need to re-run tgtsf and lynx models (the ones that use the static info, verify that the others ignore it). looks like claude on alwaystest trying to fix tgtsf_fullres caiso,jena,bear_room found an issue with how the static info is placed into the tensor cache. look at whether fixing that issue actually helps though (is a test to see if the channel specific information attending to each channel rather than being global is helpful). that can be another result actually.
- note that if reviewers aren't happy with the others ignoring static info, we could prepend it to each piece of dynamic information for those models as part of the review


---

timeline:
- I need 1 day to try enhanced on fidel-ts
- I need to get enhanced up and tested on iterated and refined by early thursday, and running on pods by thursday afternoon


---

01/21 afternoon dump:

right now (old):
- finish and test the fix to the tgtsf for multichannel broadcasting -> resubmit jobs on preemptable -> possibly move the origres tgtsf jobs from mit sloan to preemptable
- fix leret resumption problem then submit resumption jobs (submit nyc with fewer epochs: 5 pretrain, 5 fine tune, but ensure it picks up from where it left off)
- test zhanghanbest with split lr on cluster -> get zhanghanbest running (pending) on cluster
- test tensor_cache_llm on cluster -> get the tensor cache jobs going on cluster (cpu ideally)
- test and submit mmtsflib jobs with tensor cache, then timecma (mmtsflib higher priority)
- analyze results so far and work on getting high performance on my own models everywhere -- iterate fast and ensure we beat benchmarks
- submit re-runs of lynx film raw on pods for caiso, jena, and bear_room (and make a clear note in experiment tracking that the previous ones had channel descriptions all concatenated and broadcast across channels, so it wasn't clear which channel was which, but this one is clear which channel is which -- a measure of channel importance is the difference between the runs)
- submit and run lynx film raw original resolution on pods if full resolution beats tgtsf full res
- mmvision 

Tuesday night / Wednesday:
then submit zhanghanbest jobs (using the new commit with double lr -- tested and it works)
then resubmit / restart lynx_* jobs with static embeddings fix
-> the static embeddings fix is working and the tensor caches are generated, but having an embedding per channel is too large -- exceeds l40s memory. I think a better solution would be to append the channel description to the news text, but for now I can just ignore the channel embeddings in my lynx_film models (and I ignore them for tgtsf on time mmd ttc because time mmd ttc don't require them). go back to alwaystest and use the old embeddings with old stride for the loss curves for tgtsf to compare against. memory might have been why they reduced hetero stride. ohhh... i can just compare loss curves for nyc, Germany, and Canada. then I just ignore tgtsf on caiso, bear room, and jena as a benchmark to compare loss curves (the actual performance is taken from their paper). done.
-> don't resubmit lynx_* jobs with the new static embeddings fix. If anything, while messing around getting my performance up on Thursday, try cutting out their text embedder that uses channel descriptions and just don't use them at all in lynx film raw and lynx film enhanced (lynx film is out the window). just self attend and be done. no need for channel descriptions -- we learn per channel model so we are fine (ensure we do). 
-> basically don't worry about tgtsf (we have some loss curves to compare against) and don't worry about lynx_* runs anymore til we hammer out performance. 
-> benchmarks left: resume leret, zhanghanbest (submitted), mmtsflib (tensor cache llm), timecma (tensor cache llm).

then check in on cursor for leret resumption
then submit leret resumption 
then test tensor cache llm
then submit mmtsflib and timecma with tensor cache

-> we try a little bit with timecma and mmtsflib, but if it's too much, we let it go. their llm embeddings are a lot (sample-specific). We could probably get away with just citing zhanghanbest and not using the mmtsflib benchmark, though  using it would be pretty strong. just moving the damn data from one directory to another is insanely time consuming, it must be enormous.

[sleep point]

Thursday:
analysis of the results so far. go crazy on time mmd and ttc on pods getting good, consistent results. includes looking at why nyc traffic was good before and is bad now.
get good results on the small fidel ts (jena, caiso, Canada) -> this gives an abstract
submit jobs on large fidel-ts for the stuff that looks excellent on time mmd

path: 
- analysis of results so far -> make a plan
--- make sense of results -- why are some horrid and some pretty good; compare to benchmarks, and compare to previous runs (why different)
--- I can grab the results myself (useful for thinking, and make sure it's right) but then have ai in browser format into nice tables
- mit normal gpu up for running time mmd ttc quick and iterating. anything that looks good we spin up pods and run (data already there, just git fetch, pull, then run).

[sleep point]

note on hparam tuning for zhanghanbest: I can hparam tune later after other things are done -- it's not the priority right now (tuning the hparam that determines the mixing rate)


get the agent monitor working is a side quest (currently uv run stuff)

---

1/22

we have benchmarks. timecma and mmtsflib (and leret) are going to wait until the final submission probably. today is getting something solid for abstract tomorrow. hparam tuning the zhanghanbest-* and tgtsf-fullres also come after getting mine really strong.  

goals:
- beat unimodal everywhere 
- beat benchmarks everywhere
- train faster than benchmarks everywhere

investigations:
- why is ttc medical way worse now than in the 01/12 run? what is different? 
- why is nyc traffic way worse now than in sweeps? what is different? 

possibilities (investigation):
- full resolution -> more overfitting. looks like overfitting (high data -> too many batches)
- the elementwise_affine is only in lynx film enhanced, so that didn't change.
- looks very much like it is *not* full resolution because there are recent entries in the sweeps on nyc traffic that have decent val loss (like before) with full text resolution. looks like batch size, model dimension, learning rate. 
- it could be tensor cache. will find out from the run on nyc pod that will use tensor cache and the neat sweep configs

experiments [paradigm: first fix lynx film raw and lynx film before moving to lynx film enhanced]
- allow different learning rate for the film encoder, sweep over timemmd ttc, see how well we can do. log batches, and evaluate on val every few batches so we can see it (but keep early stopping at epochs for now). We can look after at whether we would have wanted to stop before end of epoch.
- run on nyc traffic the same configs as the sweep with (every n) batch level logging (of val and train loss) and tensor cache, see how it does. 
...
- test swapping out the text encoder to ignore channel descriptions and just self-attend, or try a couple other encoder types (MLP, no attention) on time mmd and ttc and a short fidel-ts (canada)
- test and run lynx_film_enhanced on time mmd and ttc and a short fidel-ts (canada)
- keeping the same film architectures in lynx film raw, lynx film, and lynx film enhanced, try applying film to patchtst


enhancements:
- gating
- stricter early stopping by step (batch) rather than epoch (requires checkpointing and validation by batch or every n batches rather than epoch)
- lower learning rate on the film modules (to reduce overfitting)

gpus:
- one for evals and timemmd ttc interactive-gpu-a (mit_normal_gpu)
- one for testing lynx_film_enhanced (mit_normal_gpu)


1/23 1 am

spinning up sweeps of lynx film enhanced (interpolative, shared). parallel doesn't work yet (just need right itransformer baseline) but shared works like a charm.
spinning up sweeps of the lynx film raw with regularization stuff as well. 

sweeps work great on time mmd ttc but don't really want to run on fidel-ts (jena)


looks like enhanced doesn't help much on small data (naturally). lynx film (non-raw) probably better on small datasets, but tuned well. 
try the hetero stride -- get back to the runs that did better.
aggressive grad clipping (grad norm spikes -> bad val loss]
less expressivity in the film, try mlp. enhanced is far too expressive -- well overfits.
looks like the seasonality is from a gradient that moved too much overfitting at a particular step. aggressive grad clipping. I think if we fix those spikes, our val loss and test loss will go waaayyy down. They become massive outliers.
i think caiso and ttc medical are drastic overfitters because it's learning per channel (per entity -- lots of entities). limiting film to apply constant across entities.

I think the things I've been thinking about will be much more important on less clean, more noisy, larger datasets with more complex longer passages of text.

not getting the exact results desired. might revert to the jan12 area and see. submit an abstract, and keep tinkering away til I get some things that are decent. need to sleep. look again after sleep at the various results. keep submitting runs. fix the entities thing and also revert back in a branch and try running things. just sleep, keep pods open and jobs open and keep running stuff. I think tensor cache fix ideally + regularization -> results. fiats honestly is pretty good. run it on time mmd ttc. my main contribution if this flops is the panel stuff which I think is really important. my own datasets and full control over standardization will also be incredibly useful. 


1/24 2 am:
- carve back to the reasoning_baselines branch -- bare bones but where I got the venv working and tgtsf running. then add lynx film raw and sweep.
- also try the grad clipping, hadn't been working. probably pretty impactful. looks like canada is going pretty decently.

lynx_film_raw_canada_photovoltaics_ema_film_20260124_022654/20260124-022654_d10b78e3e4f2 -- got canada down from .29 on test to .19 on test (basically unimodal, but still have to get to .16). might be able to increase the dimension (from 32) of the mlp hidden state for the text encoder, and/or use self attention encoder. no channel specificity on film.

maybe separate normalization per entity, particularly on canada, would be useful.

I think cls embeddings aren't great. Ideally I take entire sequence length and self attend and project as part of learning rather than cls embeddings which have little left. But, memory bottleneck.

I think weekend is carve back, replicate tgtsf, see how film fares in that environment. I have the commit hash in metadata.json for that good first run on nyc traffic (output/lynx_film_raw_test_20251209_174125/20251209-174125_829687193058/metadata.json; hash: c8e5aa7019f23ba77a171f363578f0a07e5aa9b6). would take a long time to run without tensor cache, but can go back and look at it in cursor and see what we can bring back from it (possibly using old embeddings, and the old lynx film raw implementation and dataloaders that were able to use them). 0.5 val loss on nyc traffic! -> try to recreate, then sweep w/1 epoch only (for all the fidel-ts datasets)
- could be the old (smaller) embeddings with large (768) batch size and higher (0.0008) learning rate that did the trick. carefully work into a tensor cache verifying with a test that it comes out exactly the same from the dataloader for a sample (not the entire thing -- the data loader is the bottleneck). a new tensor cache with new tests.
- bring back the model, but keep my experiment management and test during train etc (if possible -- run in both an old branch and a new branch). first priority is to branch off old, keep everything same, make sure it's tracking metrics (including test every epoch), then apply a robust tensor cache, then train everything and sweep everything. 
- welp... this one had output len 24, not 168, so the thing to beat is .4 test loss, not 0.6 test loss. we're still a ways off. I quite like the idea of bringing other datasets carefully into my own clean, standardized, rigorous repository, then benchmarking everything there.

the only things I will re-run are zhanghanbest. once lynx film raw is good on oldrun, then I re-run zhanghanbest-dlinear and zhanghanbest-patchtst for apples-to-apples comparison.

We also have lynx_film_raw_time_mmd_ttc_20260112_165034 where ttc medical was sota (output/lynx_film_raw_time_mmd_ttc_20260112_165034/20260112-180721_b6904e01040e/metadata.json contains git hash) once again, dig back and identify and bring back what worked. actually it looks like this was incorrect data aggregation. it was just as bad then as it is now. good to know. try to remove entity specificity for medical and any others that seem to overfit with many entities.

for caiso, predict only currend demand, wind, and solar. see how tgtsf and my stuff fares doing just those three.

We also have the bear room runs in experiment tracker that are sota, but that might be using non-weather info (check).

I think this was ambitious, and we made a lot of progress and learned a lot. I think good work isn't bound by 'time to publish', and my work going forward I doubt is being replicated somewhere, and if it is then great! I have something to learn from. I'll be using brand new datasets, brand new methods, and it will be more than simply incremental. I think that is where publication makes sense, if at all. This could be a workshop for now rather than a full publication, mostly for Hannah and Brianna.

I need to start making money so I can pay for tools and compute more. Build out the pipelines and research for myself. I get a strategy working first, then I come back and build out the repository, which will go toward my next event based strategies. continuing analyzing runs on the current repo is a given since I have gpus to burn and it's good to get familiar with gradient clipping and other things, though not highest priority. It's better to get a very clean, standardized, documented  repository first.

plan going forward:
- continue to keep gpus spinning and analyzing the results while I work on other things during the coming months. keep tweaking and hyperparameter tuning
- I will re-build from the ground up for my own work -- fresh repository, no heritage stuff, strict design specifications and standardization.
--- before data comes into the new repository, it will strictly adhere to the standards. there will be checks to ensure everything is in place before a dataset is allowed through. 
--- very careful and clear separation of target prediction variables and input variables not being predicted; of entity vs variables, and entity types (with variables attached)
--- very clear and careful treatment of time data available, time of event (if data is an event), time data is about (if data is something known in advance about a future time)
--- duplicate the codebase in written traces of functionality and in actual code. an src and a written (intended functionality) and tests (that check the intended functionality). specific functionalities or specific delineated entities (like an entity, or a piece of data) have their own doc page that defines them as if they were a class, then traces their (more efficient) representations all through the codebase through transformations.
--- have tests that are maintained and run that verify everything functions with pytest objects known to follow the standards for data and model before adding any real data or real models. This ensures the harness of the model and data are intact, and any issues are in the model or data (and if there is something that needs to be added to the harness, we can add it) 
--- carefully look at and understand the data. the data is the core of everything. understand the structure of the data. 
--- do good careful work first, then think about publication after. publication is a side effect of good work, not the other way around. this also lends to life.
--- very careful experiment tracking will be maintained, including mandatory git hash in output.
--- have very clear and careful documentation and distinction between the dimensions in the model, and of padding. For example text sequence dimension length vs prediction temporal input length. These should all be incredibly clearly documented and designed before any implementation. 
--- the infrastructure for using LLMs (local or API) and saving outputs must also be rigorously documented, standardized. local model registry, embeddings caches, prompt and response caches. these will be sample-specific, but for me mostly event specific so not as heavy as mmtsflib or timecma. carries in carefully with the design of event structure.
--- carefully design in what training regimes which data is considered static and which data is considered dynamic (statically embed historical financials when doing short-term post-event training)
--- keep the tensor cache -- very quick. make it fully comprehensible, fast, and memory and disk efficient.
--- much more careful project state tracking. every commit comes with a thorough markdown documentation of what was changed, and everything run is tagged with the git hash.
--- wandb tracking much more carefully. kill old runs.
--- on code changes, have a set of runs (a test experiment) that is designed (from a template) and run one epoch each for several small datasets on worktrees before and after the code changes (the same config on both worktrees) to ensure that the results are in line with expectations.
--- carefully design experiment suites (which I will now call 'experiments', and single training runs just 'runs'), and do not reuse and modify. the experiment config will be fully copied to the experiment directory, then can be deleted (manually) after completion from a temporary location to hold active experiment configs. each experiment  must be accompanied by a clear markdown that includes the reasoning for the experiment, what varies across runs, how it relates to previous experiments (what are the code changes or the config changes, never both simultaneously), and what are the hypotheses. this markdown should ideally be tracked locally and in wandb.  experiments and runs should have more comprehensible naming (like wandb if generated) and the timestamps and hashes go inside the metadata not in the names.
--- experiments will have mandatory post-experiment reviews in markdown, separate from the hypotheses and experiment design documents. they will review what occured, posit as to why, and think about follow up experiments. 
--- different cross-validation regimes, and corresponding tensor caches,  will be carefully tied to the standardized and strict data structure (i.e. cross sectional vs temporal, many fold, cross-sectional based on attributes, etc.). 
--- tensor caches should have different levels. A single dataset will have a shared cache for time series and embeddings of all types. then a run will have a run-specific cache of the indices mapping to thier samples and splits. This way the core data is not duplicated. the run-specific tensor cache mappings  are always generated before training, evaluation, etc so that the gpu is well utilized.
--- data provision always goes through the same structure with the tensor cache. there is no slow pipeline (no hetero data getter etc). datasets can be saved in their own version tracked locations, but within strict parameters and guidelines for how they are allowed to be structured, and how they are structured is givin in a clean config. there is a central tensor cache generator that uses those to create a versioned tensor cache, then that can be loaded into the trainer, evaluator, etc.
--- I am the designer, down the the software detail, and the agents are the executors. they do not implement anything I do not carefully design and fully understand.
--- when doing new things, create careful design and documentation first. for example, event based learning where the training samples are all some news output then a reaction, where we have an underlying unimodal model trained on everything, not just those event samples. that would be a very carefully crafted type of exercise that must be carefully designed and documented before construction.
--- embedder will be tied very carefully to the strict data structure and relationship with entities, variables, static vs dynamic data, and tie into the tensor cache and strict format for data on disk. embedding and tokenization model registry will remain.
--- we will maintain self-healing runs, and will maintain the smart sweep agent, but with careful design and flow specifications.
--- strictly centralized configuration management, and the config never changes type throughout. Very strict pydantic validation, no extra fields allowable.
--- zero fallbacks. everything must adhere to the strict specifications.
--- the agents instructions in .cursor, claude.md, and agents.md will be edited with a central editor command (powershell command that opens micro, then on save does xyz) that on saving will trigger copy to the respective agent instructions to maintain consistency. it will contain strict modularity guidelines, it will be appended with the structure of the repository and where to find the subdocuments that give in depth specifications on each functionality and module, it will in the header along with modularity clearly tell the agent to use zero fallbacks but put in place informative error raises and to include tracebacks on errors.
--- include visualization modules that are tied to the tensor cache. I can identify samples that are low performance or spiked error, call visualization on the run-specific cache for that sample ID, and get a clean visualization. this visualization will be strictly tied to the strict data formats
--- whether samples include all entities or just one entity is an important design consideration. plays into effective batch size, and whether batches contain a distribution across time or across a cross-section (if samples are all entities, then low amount of temporal distribution in a batch)  
--- the repository will have meta documentation for strict instructions for how an experiment should be carried out, what steps are required, what commands to run. this documentation must be rigorously maintained, and could be used eventually to have an agent generate and run experiments autonomously since the entire pipeline is strictly mapped. these agents would be fully separate from agents executing new code based on strict design guidelines. using the runpod and wandb APIs, this could be fully automated, though manually I do the entire flow first from the machine where the agent would work. Instructions for the post-mortem would need to be explicit and well documented, and I would approve the experiment before it is submitted for running.
- use learned down-projections from the text embedding side (self-attention, allowing gradient through from passing entire text sequence), and probably put that on a separate GPU (learn to manage multi-device on engaging and on runpod) for VRAM considerations (only goes to the other device for cross-attention or fusion when compressed in low rank after text self-attention)
- for this paper going forward, I think looking at what I think is actually impactful is a good idea (the embeddings type, shying away from cls or naive pooling), then showing impact across benchmarks and models. dimension of text low rank space is another that I think is interesting for a paper. conditional gating and unconditional regularization (noise) is another. unimodal integration types and how to stay parity and better is another. 
- neurips 2026 might be a stretch. first focus is passing courses and quals and making money. have summer for third year paper and good careful work, and developing a pipeline for trading simultaneously.
- when making money, adhere to the same design strictness and standards, and clear understanding of how everything works. 
- data focus in summer and pre-summer. panel work comes naturally after data. interpretability comes naturally after panel work. 3rd year paper just presentation of where I'm at (chat with David)
- noise injection and ensuring outperformance of unimodal despite noise by end of summer I also think is sensible (possibly neurips, but only if it's quality naturally by then) -- conditional gating with low capacity when it's identifiable, strong regularization, unconditional (or weakly conditional) unimodal prediction level ensemble -- once again tweak, try, and keep gpus going. maybe give myself one day a week while working on other things to debug, analyze, iterate, and carefully get more jobs going on the gpus for the coming week. be disciplined in continuing to explore.
- fashion actual experiments to run, varying things in a controlled and careful way, then look at the results.
- bring the fidel-ts and timemmd datasets into a very carefully designed new repo, they must fit the structure impeccably (later, only after it makes sense to build the new repo)
- really, focus on making money and what I really care about, and publication and academia etc falls completely secondary. publication in CS has similar issues to pub in finance. favorite method: pub when there's something genuinely interesting and useful to share, not just for the sake of publishing.


[damonp@eosloan chimera_proj]$ du -h --max-depth=2 . | sort -h
du: cannot read directory ‘./mts/data/TTC/Medical’: Permission denied
0       ./git/sha1collisiondetection
0       ./ls
4.0K    ./chronos-forecasting/ci
4.0K    ./git/sha1
4.0K    ./mts/.locks
8.0K    ./fidel-ts/text_noise_injection
12K     ./chronos-forecasting-wasserstein/.github
12K     ./damon-workspace/mts-amazon-monthly-branch
12K     ./fidel-ts/requirements
12K     ./git/bin-wrappers
12K     ./git/block-sha1
16K     ./amazon-data/docs
16K     ./fidel-ts/experiments
16K     ./fidel-ts/mit_sbatch
20K     ./chronos-forecasting/.github
20K     ./fidel-ts/examples
20K     ./fidel-ts/filtering
20K     ./mts/scripts
20K     ./mts/utils
24K     ./chronos-forecasting-wasserstein/src
24K     ./fidel-ts/.pytest_cache
24K     ./git/sha256
24K     ./git/subprojects
24K     ./MM-TSFlib/.pytest_cache
24K     ./mts/configs
28K     ./fidel-ts/analysis
28K     ./mts/__pycache__
32K     ./fidel-ts/visualization
32K     ./git/negotiator
36K     ./fidel-ts/prompt_templates
36K     ./fidel-ts/scripts
36K     ./fidel-ts/wandb
40K     ./git/ewah
40K     ./git/oss-fuzz
40K     ./MM-TSFlib/scripts
44K     ./git/.github
48K     ./mts/data_provider
56K     ./fidel-ts/docs
56K     ./Time-Series-Library/data_provider
56K     ./Time-Series-Library/exp
72K     ./MM-TSFlib/tests
76K     ./chronos-forecasting-wasserstein/test
76K     ./Time-Series-Library/utils
80K     ./git/ci
84K     ./fidel-ts/sample_indexes
88K     ./fidel-ts/notes
96K     ./git/templates
104K    ./chronos-forecasting-wasserstein/scripts
116K    ./git/mergetools
128K    ./git/sha1dc
132K    ./mts/models
136K    ./fidel-ts/model_configs
136K    ./git/xdiff
140K    ./chronos-forecasting/src
140K    ./fidel-ts/evaluation
140K    ./fidel-ts/tests
156K    ./Time-Series-Library/pic
160K    ./fidel-ts/runs
160K    ./git/trace2
164K    ./Time-Series-Library/layers
172K    ./amazon-data/lib
188K    ./MM-TSFlib/results
192K    ./chronos-forecasting/test
244K    ./fidel-ts/data_configs
248K    ./fidel-ts/future
260K    ./MM-TSFlib/data_provider
272K    ./MM-TSFlib/utils
296K    ./fidel-ts/cli
296K    ./git/reftable
300K    ./fidel-ts/configs
300K    ./Time-Series-Library/models
316K    ./git/refs
340K    ./amazon-data/logs
340K    ./git/perl
352K    ./fidel-ts/data_provider
364K    ./mmvision/preprocessed_features
372K    ./MM-TSFlib/exp
376K    ./mts/layers
380K    ./git/gitweb
388K    ./fidel-ts/utils
436K    ./fidel-ts/embedder
460K    ./fidel-ts/exp
460K    ./MM-TSFlib/layers
472K    ./fidel-ts/models
500K    ./chronos-forecasting/figures
612K    ./fidel-ts/layers
668K    ./chronos-forecasting-wasserstein/figures
856K    ./git/gitk-git
912K    ./MM-TSFlib/models
1004K   ./chronos-forecasting-wasserstein/.git
1.1M    ./Time-Series-Library/scripts
1.2M    ./git/contrib
1.3M    ./git/compat
1.5M    ./Time-Series-Library/tutorial
1.7M    ./git/git-gui
1.9M    ./chronos-forecasting-wasserstein
2.7M    ./git/builtin
3.2M    ./fidel-ts/logs
4.1M    ./damon-workspace/fidel-ts-worktree-lynx
4.4M    ./intraday/1h-bins
4.9M    ./MM-TSFlib/test_results
7.4M    ./mts/.git
7.5M    ./git/Documentation
7.9M    ./mts/results
9.0M    ./chronos-forecasting/.git
9.2M    ./mmvision/preprocessed_frames
9.3M    ./MM-TSFlib/data
9.9M    ./fidel-ts/.git
10M     ./MM-TSFlib/.git
14M     ./git/po
17M     ./git/t
21M     ./mmvision/logs
45M     ./fidel-ts/sbatch_logs
64M     ./mts/data
79M     ./Time-Series-Library/.git
82M     ./Time-Series-Library
231M    ./damon-workspace/fidel-ts-worktree-film
269M    ./git/.git
323M    ./git
421M    ./fidel-ts/HF_cache
459M    ./fidel-ts/checkpoints
526M    ./fidel-ts/LLM_cache
655M    ./from-scratch
655M    ./from-scratch/chronos-forecasting
705M    ./models
705M    ./models/run-ce-dominick
995M    ./hannah_evals/run_62632863
995M    ./hannah_evals/run_62632901
1.1G    ./chronos-forecasting
1.1G    ./chronos-forecasting/scripts
2.0G    ./hannah_evals
5.6G    ./mmvision/MMVision-worktree-branch-gatingexperts
6.0G    ./fidel-ts/output
6.4G    ./MM-TSFlib/.venv
6.5G    ./mts/.venv
6.6G    ./damon-workspace/mts-amazon
6.8G    ./damon-workspace
7.7G    ./fidel-ts/.venv
9.3G    ./fidel-ts/data
12G     ./mmvision/SEED-DV
15G     ./mts/models--meta-llama--Llama-3.1-8B-Instruct
25G     ./fidel-ts
35G     ./intraday/1m-bins
65G     ./MM-TSFlib/checkpoints
71G     ./MM-TSFlib
78G     ./chronos_datasets
78G     ./chronos_datasets/hugging_face_datasets
88G     ./amazon-data
88G     ./amazon-data/processed_datasets
261G    ./mmvision/MMVision
278G    ./mmvision
1.2T    ./mts/checkpoints
1.3T    ./intraday/raw
1.3T    ./mts
1.4T    ./intraday
3.1T    .

---

[future notes] can set patience in terms of batch steps rather than epochs, and be less patient to save compute when I am confident after a couple runs that it doesn't recover after starting to overfit
